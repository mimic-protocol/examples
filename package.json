{
  "name": "@mimicprotocol/examples",
  "private": true,
  "version": "1.0.0",
  "description": "Mimic Protocol Examples",
  "license": "GPL-3.0",
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "examples/**"
    ],
    "nohoist": [
      "**/mocha",
      "**/mocha/**"
    ]
  }
}
