version: 1.0.0
name: DCA
description: Dollar-cost averaging function that swaps tokenIn to tokenOut
inputs:
  - chainId: uint32
  - tokenIn: address
  - tokenOut: address
  - amount: string        # e.g., '1.5' = 1.5 tokenIn
  - slippageBps: uint16   # e.g., 50 = 0.50%
  - recipient: address
