version: 1.0.0
name: Swap tokens using subgraphs
description: Swap tokens querying the current price using Uniswap V2 subgraph
inputs:
  - chainId: uint32
  - tokenIn: address
  - tokenOut: address
  - slippageBps: uint16   # e.g., 50 = 0.50%
  - subgraphId: string
abis:
  - ERC20: ./abis/ERC20.json
